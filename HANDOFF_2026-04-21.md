# ITP Handoff — 2026-04-21

**From**: Claude session on `E:/Projects for MetaSphere/david-trinidad-com`
**To**: Next Claude session running **in this directory** (`E:/Projects for MetaSphere/imagine-this-printed`)
**User**: David / Money

## TL;DR — current state

| Layer | Host | Status |
|---|---|---|
| Frontend | Vercel → `imaginethisprinted.com` | ✅ live, 9 VITE_* env vars set, SSL auto |
| Backend API | Render → `api.imaginethisprinted.com` (srv-d7jpgut7vvec739bsid0) | ⚠️ LIVE but **DB CONNECTION FAILING** — see below |
| AI Worker | Render → `imagine-this-printed-worker` (srv-d7jppnn7f7vs73bb4p80) | ✅ Running, on Starter plan |
| Database | Supabase `czzyrmizvjqlifcivrhn` | ✅ awake (was paused earlier today), REST API responsive, direct DB connection broken from Render |
| DNS | Cloudflare (API token in vault) | ✅ apex, www, api all pointing at correct hosts |

**Billing**: Both Render services on Starter ($14/mo total).

## 🔴 URGENT BLOCKER — DB connection from backend

**Symptom**: `GET https://api.imaginethisprinted.com/api/health/database` returns 500 with:
```
PrismaClientInitializationError: Can't reach database server at
db.czzyrmizvjqlifcivrhn.supabase.co:5432
```

**Root cause (confirmed)**:
- `db.czzyrmizvjqlifcivrhn.supabase.co` is **IPv6-only** now (Supabase change)
- Render services are IPv4-only on outbound
- Direct-connection URL no longer works from Render

**Fix applied but not yet effective**:
- Swapped `DATABASE_URL` env var on BOTH services to the Supavisor pooler (IPv4):
  ```
  postgresql://postgres.czzyrmizvjqlifcivrhn:GJa6jLpS4Iit3dQI@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require&pgbouncer=true
  ```
- Web service redeployed after env change (status: `live`)
- **BUT** the error still references the OLD direct URL, which suggests:
  1. Runtime may still be stale (unlikely — deploy did cycle)
  2. There's another env var / Prisma config overriding `DATABASE_URL`
  3. Prisma engine binary was built for IPv6 and the client hasn't been regenerated
  4. `DIRECT_URL` or similar is also in use and still points at the old host

**Next steps**:
1. `grep -r "db\.czzyrmizvjqlifcivrhn\|DIRECT_URL\|SHADOW_DATABASE_URL" backend/` to find any other references
2. Check `backend/prisma/schema.prisma` for `directUrl` setting (might need same pooler URL)
3. On Render: trigger a **clear-cache deploy** (API: `POST /v1/services/{id}/deploys` with `clearCache: "clear"`) so Prisma regenerates
4. Tail live runtime logs via Render dashboard or API: `GET /v1/logs?ownerId=tea-d7jp7tt7vvec7392beeg&resource=srv-d7jpgut7vvec739bsid0&type=app`
5. If still stuck, try **Transaction pooler** (port 6543) instead of session (5432) — sometimes Prisma needs it

**Render API key** is in vault: `C:/Users/David/.secrets/keys.json` under `render.RENDER_API_KEY`.

## 📋 Big task — full ITP codebase audit w/ loop

**Goal**: identify every place in ITP that diverges from the current standards we've set in `david-trinidad-com` (the "Money"/"Watchtower" project), especially for image generation.

**Why**: David wants ITP's image pipeline to follow the same pattern as the david-trinidad-com `/dashboard/image-flow` module, but ITP has DTF-specific rules (transparent BG, print specs, 300 DPI, color profiles) that the generic Image Flow doesn't enforce. Current ITP image gen is a grab-bag across `backend/routes/admin/ai-products.ts`, `backend/services/replicate.ts`, `backend/services/imagination-ai.ts`, and `backend/services/vertex-ai-mockup.ts` — no shared abstraction.

**Reference implementation (READ FIRST)**:
- `E:/Projects for MetaSphere/david-trinidad-com/src/modules/image-flow/lib/models.ts` — model catalog
- `E:/Projects for MetaSphere/david-trinidad-com/src/modules/image-flow/lib/prompt-enhancer.ts` — prompt enhancer pattern
- `E:/Projects for MetaSphere/david-trinidad-com/src/app/api/image-flow/*` — API route pattern
- `E:/Projects for MetaSphere/david-trinidad-com/src/app/dashboard/image-flow/*` — UI pattern
- Memory notes:
  - `C:/Users/David/.claude/projects/E--Projects-for-MetaSphere-david-trinidad-com/memory/image_flow_loop.md`
  - `C:/Users/David/.claude/projects/E--Projects-for-MetaSphere-david-trinidad-com/memory/feedback_expose_all_model_features.md` — surface every param; no dumbed-down forms
  - `C:/Users/David/.claude/projects/E--Projects-for-MetaSphere-david-trinidad-com/memory/feedback_cost_conscious_and_revenue.md` — flag $ per call + monthly
  - `C:/Users/David/.claude/projects/E--Projects-for-MetaSphere-david-trinidad-com/memory/feedback_fal_cost_safeguards.md` — confirm $ before expensive calls (bg-remove, upscale, render)

**Scope of audit** (loop through these, one per iteration):

1. **Image models inventory**
   - Find every Replicate / Vertex / Gemini / OpenAI image call across the ITP backend
   - List: where called, which model, what params, what output format, estimated $/call
   - Flag any that don't go through a single shared wrapper (they should)

2. **DTF rule compliance**
   - DTF needs: transparent background, minimum 300 DPI, sRGB color profile, specific aspect ratios for different products (shirt, tumbler, hoodie)
   - For each image-gen path, verify these rules are enforced OR document where they're missing
   - Products that need DTF rules: DTF transfers, sublimation designs, gang sheets
   - Products that DON'T need DTF rules: product mockups, marketing imagery, hero images

3. **Prompt enhancer alignment**
   - Does ITP have a central prompt enhancer like Image Flow? If not, where does prompt shaping happen per-route?
   - Can we extract a shared enhancer with `type: "dtf" | "mockup" | "marketing"` branches?

4. **Model parameter exposure**
   - Per the `feedback_expose_all_model_features` rule, every supported model param should be surfaceable in the admin UI
   - Find forms/modals that dumb down params; flag them

5. **Cost safeguards**
   - Per `feedback_fal_cost_safeguards`: confirm $ before expensive AI calls
   - Find any silent retry loops on paid APIs (Replicate, OpenAI, Remove.bg)
   - Audit `imagination-station` pricing config vs actual call sites

6. **Frontend ↔ backend contract drift**
   - Look for response-shape mismatches (TASK_NOTES.md has history: "frontend checks `processedUrl`, backend returns `imageUrl`")
   - Flag every place that does

7. **Unused / dead code from pivots**
   - `autoQueueMockupJob` was disabled but may still be referenced
   - Railway-specific files (`railway.toml`, `nixpacks.toml`) may be stale
   - Multiple README-style MD files with overlapping content — flag for cleanup

8. **Build-time secrets leaking to client**
   - `VITE_OPENAI_API_KEY` was in `.env` (I removed it). Check for other server-only keys with `VITE_` prefix.

**Output format** per audit pass:
Create `audit-findings.md` at repo root. Each finding = one entry:
```
## [severity: high|med|low] Short title

**File(s)**: `path:line`
**Issue**: one sentence
**Recommended fix**: one sentence
**Effort**: S/M/L
```

## Auto-loop specification

Use `/loop` dynamic mode. Each iteration:
1. Pick ONE audit scope item from list above you haven't finished
2. Read the relevant files (not just grep — actually read)
3. Append findings to `audit-findings.md`
4. Commit: `docs: audit pass on <scope item>`
5. Schedule next wake in ~20–30 min via `ScheduleWakeup`

Stop when:
- All 8 scope items have at least one entry in `audit-findings.md`
- OR user says "stop"
- OR you hit 12 iterations (something's wrong, surface it)

After the audit pass finishes, write a **proposed implementation plan** to `audit-plan.md` with a ranked order of fixes by severity × effort.

**Do not make code changes** in the audit pass. Only documentation. User wants the landscape mapped first, then he'll approve which fixes to execute.

## Current deployment configuration — reference

### Vercel (frontend) — project `imagine-this-printed`
- Production domain: `imaginethisprinted.com`, `www.imaginethisprinted.com`
- Production env vars (9): all `VITE_*`, synced from `.env`
- Deploy URL pattern: `imagine-this-printed-<hash>-davids-projects-19981e4c.vercel.app`

### Render (backend)
- Web service: `srv-d7jpgut7vvec739bsid0` → `api.imaginethisprinted.com` → `imagine-this-printed-backend.onrender.com`
- Worker: `srv-d7jppnn7f7vs73bb4p80`
- Owner: `tea-d7jp7tt7vvec7392beeg`
- Repo: `https://github.com/ItMoney22/imagine-this-printed` (public)
- Root dir: `backend`
- Auto-deploy: yes, on push to `main`
- Build: `npm ci --include=dev && npx prisma generate && npm run build`
- Web start: `node dist/index.js`
- Worker start: `npm run start:worker`
- 33 env vars on each, mirrored from `backend/.env` but with:
  - `NODE_ENV=production`
  - `PORT=10000`
  - `ALLOWED_ORIGINS=https://imaginethisprinted.com,https://www.imaginethisprinted.com`
  - `PUBLIC_URL=https://api.imaginethisprinted.com`
  - `DATABASE_URL=<supabase pooler URL>` (see blocker section)

### Cloudflare DNS (zone `ac1022efbf31f1678ca335dd81a30577`)
- `imaginethisprinted.com` → CNAME `cname.vercel-dns.com` (not proxied)
- `www.imaginethisprinted.com` → CNAME `cname.vercel-dns.com` (not proxied)
- `api.imaginethisprinted.com` → CNAME `imagine-this-printed-backend.onrender.com` (not proxied)
- Cloudflare token in vault under `cloudflare.CLOUDFLARE_API_TOKEN`

### Supabase
- Project ref: `czzyrmizvjqlifcivrhn`
- Region: `us-east-1`
- REST URL: `https://czzyrmizvjqlifcivrhn.supabase.co`
- **MCP server added to `.mcp.json` but NOT YET AUTHENTICATED** — run `claude /mcp` → select supabase → authenticate (first thing in next session)

## What to do first in the next session

1. Run `claude /mcp` and authenticate Supabase MCP (unlocks SQL-level debugging)
2. Unblock the DB connection issue — this blocks everything else in prod
3. Start the audit loop

— Claude (from david-trinidad-com session)
